---
phase: 03-context-integration-mock-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/client.ts
  - frontend/src/lib/avatarUtils.ts
  - frontend/src/lib/mockData.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useSearchParams.ts
  - frontend/src/hooks/useDuration.ts
autonomous: true
requirements: [MOCK-01, MOCK-04, MOCK-05]

must_haves:
  truths:
    - "All data fetching goes through lib/api.ts abstraction layer"
    - "Mock client data includes Sarah Johnson profile accessible by UUID and phone number"
    - "Mock time slots include 3 available slots (already exist, now accessible via API layer)"
    - "URL parameters can be read via useSearchParams hook"
    - "Duration timer formats elapsed time as MM:SS"
  artifacts:
    - path: "frontend/src/types/client.ts"
      provides: "ClientData interface with contact info and optional previousJobs"
      exports: ["ClientData", "CallContext"]
    - path: "frontend/src/lib/api.ts"
      provides: "API abstraction layer — single swap point for mock to real backend"
      exports: ["getClientData", "getTimeSlots"]
    - path: "frontend/src/lib/avatarUtils.ts"
      provides: "Initials extraction and deterministic avatar color generation"
      exports: ["getInitials", "getAvatarColor"]
    - path: "frontend/src/hooks/useSearchParams.ts"
      provides: "Custom hook for reading customer_uuid, phone_number, csr_ai_phone_session_uuid from URL"
      exports: ["useSearchParams"]
    - path: "frontend/src/hooks/useDuration.ts"
      provides: "Live timer hook returning formatted MM:SS string"
      exports: ["useDuration"]
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/lib/mockData.ts"
      via: "import mockClient, mockTimeSlots"
      pattern: "import.*mockData"
    - from: "frontend/src/hooks/useSearchParams.ts"
      to: "URLSearchParams API"
      via: "window.location.search"
      pattern: "URLSearchParams.*window\\.location"
---

<objective>
Create the data foundation for Phase 3: client types, API abstraction layer, avatar utilities, URL parameter hook, and duration timer hook.

Purpose: Establish all type contracts, data access patterns, and utility hooks that the Context Panel and Audio Simulation plans depend on. The API abstraction layer (lib/api.ts) is the single swap point for replacing mock data with real backend calls.

Output: 6 files — types/client.ts, lib/api.ts, lib/avatarUtils.ts, extended mockData.ts, hooks/useSearchParams.ts, hooks/useDuration.ts
</objective>

<execution_context>
@/Users/kelvinarnold/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelvinarnold/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-context-integration-mock-engine/03-RESEARCH.md

<interfaces>
<!-- Existing types and exports the executor needs -->

From frontend/src/types/booking.ts:
```typescript
export interface ClientData {
  name: string
  phone: string
  address: string
  previousJobs: Array<{ name: string; date: string; status: string }>
}

export interface TimeSlot {
  id: string
  datetime: string
  duration: string
}
```

From frontend/src/lib/mockData.ts:
```typescript
export const mockClient: ClientData = {
  name: "Sarah Johnson",
  phone: "(303) 555-0147",
  address: "742 Oak Street, Denver, CO 80203",
  previousJobs: [
    { name: "Water Heater Repair", date: "2025-11-15", status: "Completed" },
    { name: "Pipe Leak Fix", date: "2025-08-22", status: "Completed" }
  ]
}
export const mockTimeSlots: TimeSlot[]
```

From frontend/src/contexts/ChatContext.tsx:
```typescript
type ChatState = {
  messages: ChatMessage[]
  flowState: FlowState
  bookingData: BookingData
  isTyping: boolean
  quickActionsUsed: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client types, avatar utilities, and extend mock data</name>
  <files>
    frontend/src/types/client.ts
    frontend/src/lib/avatarUtils.ts
    frontend/src/lib/mockData.ts
  </files>
  <action>
1. Create `frontend/src/types/client.ts` with:
   - `CallContext` interface: `{ queue: string; callType: string; startedAt: Date }` — represents live call metadata
   - Re-export `ClientData` from `@/types/booking` (already has name, phone, address, previousJobs)
   - Export a `ClientLookupParams` type: `{ customerUuid: string | null; phoneNumber: string | null; sessionUuid: string | null }`

2. Create `frontend/src/lib/avatarUtils.ts` with two exported functions:
   - `getInitials(name: string): string` — extract first+last initials from full name. Split on whitespace, take first char of first part and first char of last part, uppercase. Single-word names return one initial.
   - `getAvatarColor(name: string): string` — deterministic Tailwind bg color class from name hash. Use array of 6 colors: `["bg-blue-500", "bg-green-500", "bg-purple-500", "bg-pink-500", "bg-orange-500", "bg-teal-500"]`. Hash: iterate chars with `hash = charCodeAt(i) + ((hash << 5) - hash)`, index = `Math.abs(hash) % colors.length`.

3. Extend `frontend/src/lib/mockData.ts`:
   - Add `mockCallContext` export: `{ queue: "General Support", callType: "Inbound", startedAt: new Date() }`
   - The existing `mockClient` and `mockTimeSlots` stay unchanged. Import `CallContext` type from `@/types/client` for the new export. (NOTE: `CallContext` uses the re-exported `ClientData` from booking, so no circular deps.)
  </action>
  <verify>
    <automated>cd /Users/kelvinarnold/Projects/aiva/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - types/client.ts exports CallContext, re-exports ClientData, exports ClientLookupParams
    - avatarUtils.ts exports getInitials and getAvatarColor with deterministic behavior
    - mockData.ts exports mockCallContext alongside existing mockClient and mockTimeSlots
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API abstraction layer, URL params hook, and duration timer hook</name>
  <files>
    frontend/src/lib/api.ts
    frontend/src/hooks/useSearchParams.ts
    frontend/src/hooks/useDuration.ts
  </files>
  <action>
1. Create `frontend/src/hooks/` directory (new).

2. Create `frontend/src/hooks/useSearchParams.ts`:
   - Custom hook wrapping native `URLSearchParams` API
   - `useMemo` with empty deps (params read once on mount — Phase 3 spec)
   - Returns `{ customerUuid: string | null, phoneNumber: string | null, sessionUuid: string | null }` from:
     - `params.get("customer_uuid")`
     - `params.get("phone_number")`
     - `params.get("csr_ai_phone_session_uuid")`

3. Create `frontend/src/hooks/useDuration.ts`:
   - Custom hook returning `{ seconds: number, formatted: string }`
   - Uses `useState(0)` for seconds counter
   - Uses `useRef<number | null>(null)` for interval ID
   - `useEffect` with empty deps: `window.setInterval(() => setSeconds(s => s + 1), 1000)`
   - CRITICAL: Return cleanup function that calls `clearInterval` to prevent memory leaks
   - Format: `${minutes}:${secs.toString().padStart(2, "0")}` where `minutes = Math.floor(seconds / 60)` and `secs = seconds % 60`

4. Create `frontend/src/lib/api.ts` — the API abstraction layer (MOCK-01):
   - Import `mockClient`, `mockTimeSlots`, `mockCallContext` from `./mockData`
   - Import types from `@/types/client` and `@/types/booking`
   - Export `async function getClientData(customerUuid: string | null, phoneNumber: string | null): Promise<ClientData | null>`:
     - Simulate 500ms network delay: `await new Promise(resolve => setTimeout(resolve, 500))`
     - If `customerUuid === "sarah-johnson-uuid"` → return `mockClient`
     - If `phoneNumber === "(303) 555-0147"` → return `mockClient`
     - Otherwise return `null` (unknown caller)
   - Export `async function getTimeSlots(address: string): Promise<TimeSlot[]>`:
     - Simulate 600ms delay
     - Return `mockTimeSlots`
   - Export `async function getCallContext(): Promise<CallContext>`:
     - Simulate 300ms delay
     - Return `mockCallContext`
   - Add commented-out section showing future real API pattern for documentation
  </action>
  <verify>
    <automated>cd /Users/kelvinarnold/Projects/aiva/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - lib/api.ts exports getClientData, getTimeSlots, getCallContext with simulated delays
    - getClientData resolves "sarah-johnson-uuid" and "(303) 555-0147" to mockClient, null otherwise
    - hooks/useSearchParams.ts reads customer_uuid, phone_number, csr_ai_phone_session_uuid from URL
    - hooks/useDuration.ts returns formatted MM:SS string with proper interval cleanup
    - All data fetching now has a single swap-out point (lib/api.ts)
    - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero TypeScript errors
2. `cd frontend && npm run build` — build succeeds
3. Verify lib/api.ts is the single import point for mock data (no direct mockData imports from components)
4. Verify useSearchParams reads all 3 URL parameters
5. Verify useDuration has cleanup in useEffect return
</verification>

<success_criteria>
- API abstraction layer (lib/api.ts) provides async functions with simulated delays for all data access
- Client data resolvable by both customer_uuid and phone_number
- Avatar utility generates deterministic colors and correct initials
- URL parameter hook reads 3 params from query string
- Duration hook formats time as MM:SS with interval cleanup
- Zero TypeScript errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-integration-mock-engine/03-01-SUMMARY.md`
</output>
