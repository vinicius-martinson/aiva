---
phase: 01-foundation-chat-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/components.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/NavItem.tsx
  - frontend/src/components/layout/NavGroup.tsx
  - frontend/src/components/layout/UserFooter.tsx
  - frontend/src/components/layout/ContextPanel.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/components/ui/avatar.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/separator.tsx
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
must_haves:
  truths:
    - "Product name is 'Aiva' (not FieldPro) — sidebar logo must say 'Aiva'"
    - "Three-column layout uses CSS grid: grid-cols-[260px_1fr_300px] h-screen"
    - "Sidebar background uses design system sidebar tokens ($--sidebar, $--sidebar-foreground)"
    - "Active nav state: bg-gray-900 text-white on 'AI Assistant'"
    - "Inactive nav state: text-only with hover highlight"
    - "Nav groups: 'Main' (Dashboard, AI Assistant), ungrouped (Jobs, Schedule, Clients), 'More' (Settings)"
    - "Icons: Lucide — LayoutDashboard, Sparkles, Briefcase, Calendar, Users, Settings"
    - "User footer: avatar initials circle + display name + email + kebab menu (non-functional)"
    - "Right context panel is a visible placeholder in Phase 1 — reserved space, not populated"
    - "Desktop only, min 1280px viewport — no responsive breakpoints required"
  artifacts:
    - "docs/csr-ai-implementation-plan.md — visual and layout source of truth"
  key_links: []
---

<objective>
Scaffold the Vite + React + TypeScript frontend project with Tailwind CSS, Shadcn/ui, and Lucide icons. Build the three-column desktop layout shell with a fully styled left sidebar (Aiva branding, grouped navigation, user footer) and a right context panel placeholder.

Purpose: Establish the project foundation and visual shell that all chat components will render within.
Output: A running `npm run dev` app showing three columns — left sidebar with active "AI Assistant" nav, empty center column, and placeholder right panel.
</objective>

<execution_context>
@/Users/kelvinarnold/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelvinarnold/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with Tailwind, Shadcn/ui, and Lucide</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/index.css
    frontend/src/lib/utils.ts
    frontend/components.json
    frontend/tailwind.config.js
    frontend/postcss.config.js
    frontend/src/components/ui/avatar.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/separator.tsx
  </files>
  <action>
    1. From the repo root, run:
       ```bash
       npm create vite@latest frontend -- --template react-ts
       ```
    2. `cd frontend && npm install`
    3. Install Tailwind CSS:
       ```bash
       npm install -D tailwindcss postcss autoprefixer
       npx tailwindcss init -p
       ```
    4. Configure `tailwind.config.js`:
       ```js
       export default {
         content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
         theme: { extend: {} },
         plugins: [],
       }
       ```
    5. Replace `src/index.css` with:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```
    6. Initialize Shadcn/ui:
       ```bash
       npx shadcn@latest init
       ```
       Configuration: Style=Default, Base color=Slate, CSS variables=Yes, TypeScript=Yes, Import alias=@/components.
       This creates `components.json`, `src/lib/utils.ts` with `cn()`, and updates `tsconfig.json` with path aliases.
    7. Verify `vite.config.ts` has the `@` path alias:
       ```ts
       import path from "path"
       import { defineConfig } from "vite"
       import react from "@vitejs/plugin-react"

       export default defineConfig({
         plugins: [react()],
         resolve: {
           alias: {
             "@": path.resolve(__dirname, "./src"),
           },
         },
       })
       ```
    8. Install Lucide icons: `npm install lucide-react`
    9. Add Shadcn components needed for this plan:
       ```bash
       npx shadcn@latest add button avatar separator
       ```
    10. Clear out default Vite boilerplate: remove `src/App.css`, clear `src/App.tsx` to a minimal placeholder.
    11. Verify: `npm run dev` starts without errors.
  </action>
  <verify>
    ```bash
    cd frontend && npm run build
    ```
    Build succeeds with zero errors. `src/components/ui/button.tsx`, `src/components/ui/avatar.tsx`, and `src/lib/utils.ts` all exist.
  </verify>
  <done>
    - `frontend/` directory exists with Vite + React + TypeScript
    - Tailwind CSS processes utility classes
    - Shadcn/ui initialized with button, avatar, separator components
    - Lucide React installed
    - `@/` import alias resolves correctly
    - `npm run dev` starts without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build left sidebar with Aiva branding, grouped navigation, and user footer</name>
  <files>
    frontend/src/components/layout/Sidebar.tsx
    frontend/src/components/layout/NavItem.tsx
    frontend/src/components/layout/NavGroup.tsx
    frontend/src/components/layout/UserFooter.tsx
  </files>
  <action>
    Create four files in `frontend/src/components/layout/`:

    **NavItem.tsx** — Reusable nav link component:
    ```tsx
    import { LucideIcon } from "lucide-react"
    import { cn } from "@/lib/utils"

    type NavItemProps = {
      icon: LucideIcon
      label: string
      href: string
      active?: boolean
    }

    export function NavItem({ icon: Icon, label, href, active }: NavItemProps) {
      return (
        <a
          href={href}
          className={cn(
            "flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium transition-colors",
            active
              ? "bg-gray-900 text-white"
              : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground"
          )}
        >
          <Icon className="h-4 w-4" />
          <span>{label}</span>
        </a>
      )
    }
    ```

    **NavGroup.tsx** — Labeled group wrapper:
    ```tsx
    import { ReactNode } from "react"

    type NavGroupProps = {
      label: string
      children: ReactNode
    }

    export function NavGroup({ label, children }: NavGroupProps) {
      return (
        <div className="mb-4">
          <h3 className="px-3 mb-2 text-xs font-semibold text-sidebar-foreground/50 uppercase tracking-wider">
            {label}
          </h3>
          <div className="space-y-1">{children}</div>
        </div>
      )
    }
    ```

    **UserFooter.tsx** — Bottom user section:
    ```tsx
    import { Avatar, AvatarFallback } from "@/components/ui/avatar"
    import { MoreVertical } from "lucide-react"

    export function UserFooter() {
      return (
        <div className="flex items-center gap-3">
          <Avatar className="h-8 w-8">
            <AvatarFallback className="bg-blue-500 text-white text-xs">
              KA
            </AvatarFallback>
          </Avatar>
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium truncate">Kelvin Arnold</p>
            <p className="text-xs text-sidebar-foreground/50 truncate">kelvin@example.com</p>
          </div>
          <button className="p-1 hover:bg-sidebar-accent rounded" aria-label="User menu">
            <MoreVertical className="h-4 w-4 text-sidebar-foreground/50" />
          </button>
        </div>
      )
    }
    ```

    **Sidebar.tsx** — Assembles logo, nav groups, and user footer:
    ```tsx
    import {
      LayoutDashboard,
      Sparkles,
      Briefcase,
      Calendar,
      Users,
      Settings,
    } from "lucide-react"
    import { NavItem } from "./NavItem"
    import { NavGroup } from "./NavGroup"
    import { UserFooter } from "./UserFooter"
    import { Separator } from "@/components/ui/separator"

    export function Sidebar() {
      return (
        <aside className="flex flex-col h-full bg-sidebar text-sidebar-foreground border-r">
          {/* Logo */}
          <div className="px-4 py-6">
            <div className="flex items-center gap-2">
              <div className="h-8 w-8 rounded-lg bg-blue-600 flex items-center justify-center">
                <Sparkles className="h-4 w-4 text-white" />
              </div>
              <span className="text-lg font-semibold">Aiva</span>
            </div>
          </div>

          {/* Navigation */}
          <nav className="flex-1 px-3 overflow-y-auto">
            <NavGroup label="Main">
              <NavItem icon={LayoutDashboard} label="Dashboard" href="/dashboard" />
              <NavItem icon={Sparkles} label="AI Assistant" href="/ai-assistant" active />
            </NavGroup>

            <div className="space-y-1 mb-4">
              <NavItem icon={Briefcase} label="Jobs" href="/jobs" />
              <NavItem icon={Calendar} label="Schedule" href="/schedule" />
              <NavItem icon={Users} label="Clients" href="/clients" />
            </div>

            <NavGroup label="More">
              <NavItem icon={Settings} label="Settings" href="/settings" />
            </NavGroup>
          </nav>

          {/* User footer */}
          <Separator />
          <div className="px-4 py-3">
            <UserFooter />
          </div>
        </aside>
      )
    }
    ```

    Key implementation notes:
    - "AI Assistant" is the ONLY active nav item (`active` prop = true)
    - Product name is "Aiva" — NOT "FieldPro"
    - Use Shadcn sidebar CSS variable tokens (bg-sidebar, text-sidebar-foreground) where available; fall back to explicit Tailwind classes if Shadcn theme doesn't define sidebar tokens yet
    - Kebab menu button is non-functional — just renders the icon
  </action>
  <verify>
    Import `Sidebar` into `App.tsx` temporarily and run `npm run dev`. Visually confirm:
    - Aiva logo with blue icon top-left
    - "Main" group with Dashboard and AI Assistant (AI Assistant has dark bg, white text)
    - Ungrouped: Jobs, Schedule, Clients
    - "More" group with Settings
    - User footer at bottom with initials, name, email, kebab icon
  </verify>
  <done>
    - Sidebar renders with "Aiva" branding (not FieldPro)
    - Nav groups "Main" and "More" are labeled, ungrouped items render without a label
    - "AI Assistant" has active state: dark filled background with white text
    - All other nav items have inactive state with hover highlight
    - User footer shows avatar initials, display name, email, and kebab menu icon
    - All 6 Lucide icons render correctly (LayoutDashboard, Sparkles, Briefcase, Calendar, Users, Settings)
  </done>
</task>

<task type="auto">
  <name>Task 3: Build three-column layout shell with context panel placeholder</name>
  <files>
    frontend/src/components/layout/ContextPanel.tsx
    frontend/src/components/layout/AppLayout.tsx
    frontend/src/App.tsx
  </files>
  <action>
    Create the layout components:

    **ContextPanel.tsx** — Right panel placeholder for Phase 1:
    ```tsx
    export function ContextPanel() {
      return (
        <aside className="flex flex-col h-full bg-background border-l">
          <div className="px-4 py-4 border-b">
            <h2 className="text-sm font-semibold">Call Context</h2>
          </div>
          <div className="flex-1 flex items-center justify-center p-6">
            <p className="text-sm text-muted-foreground text-center">
              Waiting for call...
            </p>
          </div>
        </aside>
      )
    }
    ```

    **AppLayout.tsx** — Three-column grid layout:
    ```tsx
    import { ReactNode } from "react"
    import { Sidebar } from "./Sidebar"
    import { ContextPanel } from "./ContextPanel"

    type AppLayoutProps = {
      children: ReactNode
    }

    export function AppLayout({ children }: AppLayoutProps) {
      return (
        <div className="grid grid-cols-[260px_1fr_300px] h-screen bg-background">
          <Sidebar />
          <main className="flex flex-col h-screen min-w-0">
            {children}
          </main>
          <ContextPanel />
        </div>
      )
    }
    ```

    **App.tsx** — Wire it all together:
    ```tsx
    import { AppLayout } from "@/components/layout/AppLayout"

    function App() {
      return (
        <AppLayout>
          <div className="flex-1 flex items-center justify-center">
            <p className="text-muted-foreground">Chat area — implemented in Plan 02</p>
          </div>
        </AppLayout>
      )
    }

    export default App
    ```

    Key implementation notes:
    - `grid-cols-[260px_1fr_300px]` gives exact sidebar widths with flex center
    - `h-screen` fills the entire viewport
    - `min-w-0` on main prevents flex overflow issues
    - Context panel shows "Call Context" header and "Waiting for call..." placeholder — this is adequate for Phase 1
  </action>
  <verify>
    Run `npm run dev` and verify in a browser (at least 1280px wide):
    - Three distinct columns are visible
    - Left sidebar is approximately 260px with full nav
    - Center column fills remaining space
    - Right panel is approximately 300px with "Call Context" header and placeholder text
    - Full viewport height, no scrollbar on the page itself
    Run `npm run build` — zero errors.
  </verify>
  <done>
    - Three-column layout renders: left sidebar (~260px), center chat area (flex-1), right context panel (~300px)
    - Sidebar, center, and context panel each fill the full viewport height
    - Context panel shows "Call Context" header with "Waiting for call..." placeholder
    - No horizontal overflow or layout collapse at 1280px viewport width
    - LAYOUT-01 satisfied: three-column desktop layout renders correctly
    - LAYOUT-02 satisfied: sidebar shows Aiva logo, grouped nav items, and user footer
    - LAYOUT-03 satisfied: AI Assistant nav item shows active state
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — zero TypeScript errors, zero build errors
2. `npm run dev` renders three-column layout in the browser
3. Left sidebar shows: Aiva logo, Main group (Dashboard, AI Assistant active), ungrouped (Jobs, Schedule, Clients), More group (Settings), user footer
4. Right panel shows "Call Context" header with placeholder content
5. Center column is empty but structurally ready for chat components
</verification>

<success_criteria>
- LAYOUT-01: Three columns visible at 1280px+ viewport — sidebar ~260px, center flex, panel ~300px
- LAYOUT-02: Sidebar has Aiva logo, 6 nav items in correct groups, user footer with initials/name/email
- LAYOUT-03: "AI Assistant" nav item has dark background + white text active state
- `npm run build` passes with zero errors
- `npm run dev` renders the complete layout shell
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-chat-ui/01-01-SUMMARY.md`
</output>
